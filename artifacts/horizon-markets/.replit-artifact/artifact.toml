kind = "web"
previewPath = "/"
title = "HedgeGate"
version = "1.0.0"
id = "artifacts/horizon-markets"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25102

[services.development]
run = "pnpm --filter @workspace/horizon-markets run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/horizon-markets", "run", "build" ]
publicDir = "artifacts/horizon-markets/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25102"
BASE_PATH = "/"
